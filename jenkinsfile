pipeline {
    agent any

    stages {
        stage('compile') {
            steps {
                sh 'javac Index.java'
            }
        }
        stage('run') {
            steps {
                sh 'java Index.java'
            }
        }
    }
}